---
import LayoutDefault from '~/layouts/LayoutDefault.astro'
import { Image } from 'astro:assets';

import {Content as About_Content_Main} from '~/pages/about_main.md'
import {Content as About_Content} from '~/pages/about.md'
import {Content as About_Content_Recent_Research} from '~/pages/about_recent_research.md'
import {Content as About_Content_Contact} from '~/pages/about_contact.md'
import {Content as About_Content_News} from '~/pages/about_news.mdx'
import {Content as Links} from '~/pages/related_links.mdx'

import placeholder_img from '~/pages/research_publications_mdx/placeholder_0.png';
import placeholder_1_img from '~/pages/research_publications_mdx/placeholder_1.png';
import eccv2026_straight_path_flow_matching_img from '~/pages/research_publications_mdx/eccv2026_straight_path_flow_matching.png';
import flowertune_img from '~/pages/research_publications_mdx/flowertune.png';
import frame_wise_conditioning_img from '~/pages/research_publications_mdx/frame-wise_conditioning.png';
import eccv2024_0_colab_img from '~/pages/research_publications_mdx/eccv2024_0_colab.png';
import anu_logo_img from '~/pages/research_publications_mdx/anu_logo_0.png';
import tmlr2024_0_img from '~/pages/research_publications_mdx/tmlr2024_0.png';
import wacv2024_0_img from '~/pages/research_publications_mdx/wacv2024_0.png';
import iccv2023_0_colab_img from '~/pages/research_publications_mdx/iccv2023_0_colab.png';
import cvpr2023_0_colab_img from '~/pages/research_publications_mdx/cvpr2023_0_colab.png';
import iccv2021_0_img from '~/pages/research_publications_mdx/iccv2021_0.png';

import {Content as Research_eccv2026_straight_path_flow_matching} from '~/pages/research_publications_mdx/eccv2026_straight_path_flow_matching.mdx'
import {Content as Research_flowertune} from '~/pages/research_publications_mdx/flowertune.mdx'
import {Content as Research_frame_wise_conditioning} from '~/pages/research_publications_mdx/frame-wise_conditioning.mdx'
import {Content as Research_eccv2024_0_colab} from '~/pages/research_publications_mdx/eccv2024_0_colab.mdx'
import {Content as Research_phd_thesis} from '~/pages/research_publications_mdx/phd_thesis_0.mdx'
import {Content as Research_tmlr2024_0} from '~/pages/research_publications_mdx/tmlr2024_0.mdx'
import {Content as Research_wacv2024_0} from '~/pages/research_publications_mdx/wacv2024_0.mdx'
import {Content as Research_iccv2023_0_colab} from '~/pages/research_publications_mdx/iccv2023_0_colab.mdx'
import {Content as Research_cvpr2023_0_colab} from '~/pages/research_publications_mdx/cvpr2023_0_colab.mdx'
import {Content as Research_iccv2021_0} from '~/pages/research_publications_mdx/iccv2021_0.mdx'


const { translate: t } = Astro.locals

// const apiUrl_time = "https://timeapi.io/api/Time/current/zone?timeZone=Australia/Adelaide"

---
  <LayoutDefault disableIntroAnimations>
  <script is:inline>
    document.documentElement.classList.add('home-progressive-js');
  </script>
  <main data-home-progressive-column data-home-progressive-pending="true" un-lg="py-20 min-w-xl" class="home-progressive-text py-7.5 px-4 md:p-7.5"> 
  
    <div id="profile-top" class="grid gap-4 items-stretch" style="grid-template-columns:120px minmax(0,1fr);">
    <!-- <div class="flex grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3"> -->

      <div>
        <img class="rounded" style="display:block;width:120px;max-width:120px;height:auto;" src="/zheyuan_david_avatar_4_5.png" alt="profile picture"> 
      </div>

      <div class="min-w-0" style="height:100%;position:relative;">
        <article
          class="heti"
          data-hscroll-panel
          style="height:100%;max-width:100%;overflow-x:auto;overflow-y:hidden;padding-right:1.2rem;box-sizing:border-box;scrollbar-gutter:stable;"
        >
          <div data-hscroll-content style="display:inline-block;min-width:max-content;white-space:nowrap;">
            <div style="margin-bottom:0.35em;font-size: 12pt;font-weight: 600;">Research Interest</div>
            <ul style="margin: 0 0 0.6em 1.2em; font-size: 11.5pt;">
              <li>large language model;</li>
              <li>video generative / video editing models;</li>
              <li>vision-and-language, multi-modal learning.</li>
            </ul>

            <div style="margin-bottom:0.2em;font-size: 11.5pt;"><span class="i-mdi:email-arrow-left">Icon</span>zheyuan.david.liu [at] outlook.com</div>
            <!-- <div style="margin-bottom:0.2em;font-size: 11.5pt;"><span class="i-mdi:contact-phone">Icon</span><a href="https://openreview.net/profile?id=%7EZheyuan_Liu2">OpenReview Profile</a></div> -->
            <!-- <div style="margin-bottom:0.2em;font-size: 11.5pt;"><span class="i-mdi:file-download">Icon</span><a href="cv_plain.html">Curriculum Vitae</a></div> -->
          </div>
      
        </article>
        <div
          data-hscroll-indicator
          aria-hidden="true"
          style="position:absolute;top:0;right:0;height:100%;width:1.4rem;display:flex;align-items:center;justify-content:flex-end;padding-right:0.2rem;background:linear-gradient(to right, rgba(255,255,255,0), rgba(255,255,255,0.88));color:rgba(71,85,105,0.72);font-size:15px;pointer-events:none;opacity:0;visibility:hidden;transition:opacity 0.16s ease;"
        >
          &#8250;
        </div>
      </div>

    </div>
  <section id="about" data-home-progressive-section contain-layout un-flex="~ col gap-7.5">
    <article class="heti">
      </br>
      </br>
      <About_Content_Main />
    </article>
  </section>  
  
  <section data-home-progressive-section contain-layout un-flex="~ col gap-7.5">
    <article class="heti">
      </br>
      <About_Content />
    </article>
  </section>  

  <section id="recent-research-section" data-home-progressive-section contain-layout un-flex="~ col gap-7.5">
    <article class="heti">
      </br>
      <About_Content_Recent_Research />
    </article>
  </section>  

  <section id="news" data-home-progressive-section contain-layout un-flex="~ col gap-7.5">
    <article class="heti">
      <About_Content_News />
    </article>
    <hr class="hr-heti" />
  </section>  

  <section id="recent-research" data-home-progressive-section contain-layout un-flex="~ col gap-7.5">
    <article class="heti">
      <h5 class="section-heading">Recent Research</h5>
      
      <div id="recent-research">
        <div class="w-full overflow-hidden">
          <div class="md:flex">
            <div class="pt-3 pr-3 pb-3 -mb-6 md:shrink-0">
              <Image class="mt-3.5 rounded-md shadow-sm w-48 mx-auto object-cover hover:shadow-lg" style="transition:all .3s ease-in-out;" src={eccv2026_straight_path_flow_matching_img} alt="straight_path_flow_matching_img"> 
            </div>
            <div class="pt-3 pr-3 pb-3 leading-tight text-slate-700">
              <Research_eccv2026_straight_path_flow_matching />
            </div>
          </div>
        </div>

        <div class="w-full overflow-hidden">
          <div class="md:flex">
            <div class="pt-3 pr-3 pb-3 -mb-6 md:shrink-0">
              <Image class="mt-3.5 rounded-md shadow-sm w-48 mx-auto object-cover hover:shadow-lg" style="transition:all .3s ease-in-out;" src={flowertune_img} alt="flowertune_img"> 
            </div>
            <div class="pt-3 pr-3 pb-3 leading-tight text-slate-700">
              <Research_flowertune />
            </div>
          </div>
        </div>

        <div class="w-full overflow-hidden">
          <div class="md:flex">
            <div class="pt-3 pr-3 pb-3 -mb-6 md:shrink-0">
              <Image class="mt-3.5 rounded-md shadow-sm w-48 mx-auto object-cover hover:shadow-lg" style="transition:all .3s ease-in-out;" src={frame_wise_conditioning_img} alt="frame_wise_conditioning_img"> 
            </div>
            <div class="pt-3 pr-3 pb-3 leading-tight text-slate-700">
              <Research_frame_wise_conditioning />
            </div>
          </div>
        </div>
        <div class="text-gray-500 collapse-toggle-wrap" style="transition:all .3s ease-in-out; padding-top: 15px">
          <button
            type="button"
            data-collapse-target="collapsible-content"
            data-expand-label="See more"
            data-collapse-label="See fewer"
            aria-expanded="false"
          >
            <span class="label">See more</span>
          </button>
        </div>
      </div>

      <div id="collapsible-content" class="collapse-content collapse-content--research">

        <div class="w-full overflow-hidden">
          <div class="md:flex">
            <div class="pt-3 pr-3 pb-3 -mb-6 md:shrink-0">
              <Image class="mt-3.5 rounded-md shadow-sm w-48 mx-auto object-cover hover:shadow-lg" style="transition:all .3s ease-in-out;" src={eccv2024_0_colab_img} alt="eccv2024_0_colab_img"> 
            </div>
            <div class="pt-3 pr-3 pb-3 leading-tight text-slate-700">
              <Research_eccv2024_0_colab />
            </div>
          </div>
        </div>

        <div class="w-full overflow-hidden">
          <div class="md:flex">
            <div class="pt-3 pr-3 pb-3 -mb-6 md:shrink-0">
              <Image class="mt-3.5 rounded-md shadow-sm w-48 mx-auto object-cover hover:shadow-lg" style="transition:all .3s ease-in-out;" src={anu_logo_img} alt="anu_logo_img"> 
            </div>
            <div class="pt-3 pr-3 pb-3 leading-tight text-slate-700">
              <Research_phd_thesis />
            </div>
          </div>
        </div>

        <div class="w-full overflow-hidden">
          <div class="md:flex">
            <div class="pt-3 pr-3 pb-3 -mb-6 md:shrink-0">
              <Image class="mt-3.5 rounded-md shadow-sm w-48 mx-auto object-cover hover:shadow-lg" style="transition:all .3s ease-in-out;" src={tmlr2024_0_img} alt="tmlr2024_0_img"> 
            </div>
            <div class="pt-3 pr-3 pb-3 leading-tight text-slate-700">
              <Research_tmlr2024_0 />
            </div>
          </div>
        </div>

        <div class="w-full overflow-hidden">
          <div class="md:flex">
            <div class="pt-3 pr-3 pb-3 -mb-6 md:shrink-0">
              <Image class="mt-3.5 rounded-md shadow-sm w-48 mx-auto object-cover hover:shadow-lg" style="transition:all .3s ease-in-out;" src={wacv2024_0_img} alt="wacv2024_0_img"> 
            </div>
            <div class="pt-3 pr-3 pb-3 leading-tight text-slate-700">
              <Research_wacv2024_0 />
            </div>
          </div>
        </div>

        <div class="w-full overflow-hidden">
          <div class="md:flex">
            <div class="pt-3 pr-3 pb-3 -mb-6 md:shrink-0">
              <Image class="mt-3.5 rounded-md shadow-sm w-48 mx-auto object-cover hover:shadow-lg" style="transition:all .3s ease-in-out;" src={iccv2023_0_colab_img} alt="iccv2023_0_colab_img"> 
            </div>
            <div class="pt-3 pr-3 pb-3 leading-tight text-slate-700">
              <Research_iccv2023_0_colab />
            </div>
          </div>
        </div>

        <div class="w-full overflow-hidden">
          <div class="md:flex">
            <div class="pt-3 pr-3 pb-3 -mb-6 md:shrink-0">
              <Image class="mt-3.5 rounded-md shadow-sm w-48 mx-auto object-cover hover:shadow-lg" style="transition:all .3s ease-in-out;" src={cvpr2023_0_colab_img} alt="cvpr2023_0_colab_img"> 
            </div>
            <div class="pt-3 pr-3 pb-3 leading-tight text-slate-700">
              <Research_cvpr2023_0_colab />
            </div>
          </div>
        </div>

        <div class="w-full overflow-hidden">
          <div class="md:flex">
            <div class="pt-3 pr-3 pb-3 -mb-6 md:shrink-0">
              <Image class="mt-3.5 rounded-md shadow-sm w-48 mx-auto object-cover hover:shadow-lg" style="transition:all .3s ease-in-out;" src={iccv2021_0_img} alt="iccv2021_0_img"> 
            </div>
            <div class="pt-3 pr-3 pb-3 leading-tight text-slate-700">
              <Research_iccv2021_0 />
            </div>
          </div>
        </div>
      </div>
      <hr class="hr-heti" />
    </article>
  </section> 

  <section id="affiliations" data-home-progressive-section contain-layout un-flex="~ col gap-7.5">
    <article class="heti">
      <h5 class="section-heading">Affiliations</h5>
      <div class="affiliations">
        <div class="affiliation-timeline">
          <div class="affiliation-item">
            <div class="affiliation-logo affiliation-logo--text hover:shadow-lg" style="transition:all .3s ease-in-out;">2025</div>
            <div class="affiliation-text">
              <div class="affiliation-meta">
                <span class="affiliation-name">Oracle Health AI</span>
                <span class="affiliation-location">Melbourne, Australia</span>
              </div>
              <details class="affiliation-role-details">
                <summary>Senior Applied Scientist</summary>
                <div class="affiliation-role-details-body">
                  <div class="affiliation-expertise-label">Area of Research</div>
                  <div>Large Language Model</div>
                  <div class="affiliation-expertise-label affiliation-expertise-label--spaced">Area of Expertise / Domain Knowledge</div>
                  <div>Medical AI, Clinical Revenue Cycle</div>
                  <ul class="affiliation-expertise-list">
                    <li>Clinical Documentation Improvement (CDI)</li>
                    <li>Healthcare Reimbursement</li>
                    <li>Clinical Coding</li>
                  </ul>
                </div>
              </details>
            </div>
          </div>
          <div class="affiliation-item">
            <div class="affiliation-logo affiliation-logo--text hover:shadow-lg" style="transition:all .3s ease-in-out;">2024</div>
            <div class="affiliation-text">
              <div class="affiliation-meta">
                <span class="affiliation-name">Australian Institute for Machine Learning (AIML)</span>
                <span class="affiliation-location">Adelaide, Australia</span>
              </div>
              <details class="affiliation-role-details">
                <summary>Postdoctoral Research Fellow</summary>
                <div class="affiliation-role-details-body">
                  <div class="affiliation-expertise-label">Area of Research</div>
                  <div>Video generative / video editing models</div>
                  <div>Efficient fine-tuning</div>
                  <div>Adapter design</div>
                </div>
              </details>
            </div>
          </div>
          <div class="affiliation-item">
           <div class="affiliation-logo affiliation-logo--text hover:shadow-lg" style="transition:all .3s ease-in-out;">2015</div>
            <div class="affiliation-text">
              <div class="affiliation-meta">
                <span class="affiliation-name">Australian National University (ANU)</span>
                <span class="affiliation-location">Canberra, Australia</span>
              </div>
              <div class="affiliation-role-group">
                <details class="affiliation-role-details">
                  <summary>PhD Student</summary>
                  <div class="affiliation-role-details-body">
                    <div class="affiliation-expertise-label">Area of Research</div>
                    <div>Vision-language</div>
                    <ul class="affiliation-expertise-list">
                      <li>Composed image retrieval</li>
                    </ul>
                    <div>Multi-modal learning</div>
                  </div>
                </details>
                <div class="affiliation-role-secondary">Undergraduate Student</div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <hr class="hr-heti" />
    </article>
  </section>  

  <section id="links" data-home-progressive-section contain-layout un-flex="~ col gap-7.5">
    <article class="heti">
      <h5 class="section-heading">Related Links</h5>
      <Links />
    </article>
  </section>  

  </main>
  <button id="back-to-top" class="back-to-top" type="button" aria-label="Return to top" aria-hidden="true" hidden>
    <svg viewBox="0 0 24 24" role="img" aria-hidden="true" focusable="false">
      <path d="M12 18V6M7 11l5-5 5 5" />
    </svg>
  </button>
  <script is:inline>
    (() => {
      const bindProgressiveText = () => {
        const root = document.querySelector('[data-home-progressive-column]');
        if (!(root instanceof HTMLElement)) {
          return;
        }

        const reduceMotion = () => window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        const progressiveTargetSelector = '[data-home-progressive-text="true"], [data-home-progressive-visual="true"]';
        const startElement = (element, delayOverride) => {
          if (!(element instanceof HTMLElement) || element.dataset.homeProgressiveState !== 'waiting') {
            return;
          }

          if (delayOverride != null) {
            element.style.setProperty('--home-op-delay', `${delayOverride}ms`);
          }

          element.dataset.homeProgressiveState = reduceMotion() ? 'entered' : 'playing';
        };
        const playRevealedContent = (content) => {
          if (!(content instanceof HTMLElement)) {
            return;
          }

          const revealedTargets = content.matches(progressiveTargetSelector)
            ? [content]
            : [];
          revealedTargets.push(...content.querySelectorAll(progressiveTargetSelector));

          revealedTargets.forEach((element, index) => {
            startElement(element, index * 32);
          });
        };
        const bindCollapseRevealAnimations = () => {
          root.querySelectorAll('[data-collapse-target]').forEach((button) => {
            if (!(button instanceof HTMLElement) || button.dataset.homeProgressiveCollapseBound === 'true') {
              return;
            }

            button.dataset.homeProgressiveCollapseBound = 'true';
            button.addEventListener('click', () => {
              const targetId = button.getAttribute('data-collapse-target');
              if (!targetId) {
                return;
              }

              window.requestAnimationFrame(() => {
                const content = document.getElementById(targetId);
                if (content instanceof HTMLElement && content.classList.contains('open')) {
                  playRevealedContent(content);
                }
              });
            });
          });
        };
        const activate = () => {
          const targets = root.querySelectorAll(progressiveTargetSelector);

          if (!('IntersectionObserver' in window)) {
            targets.forEach((element) => startElement(element));
            return;
          }

          const observer = new IntersectionObserver((entries) => {
            entries
              .filter((entry) => entry.isIntersecting)
              .sort((a, b) => a.boundingClientRect.top - b.boundingClientRect.top)
              .forEach((entry, index) => {
                observer.unobserve(entry.target);
                startElement(entry.target, index * 32);
              });
          }, { threshold: 0.18 });

          targets.forEach((element) => {
            if (element instanceof HTMLElement && element.dataset.homeProgressiveState === 'waiting') {
              observer.observe(element);
            }
          });
        };
        const reveal = () => {
          root.removeAttribute('data-home-progressive-pending');
        };
        const revealAndActivate = () => {
          reveal();
          bindCollapseRevealAnimations();
          activate();
        };
        const whenTypographyReady = (callback) => {
          if (document.fonts?.ready) {
            document.fonts.ready.then(callback, callback);
            return;
          }

          callback();
        };

        if (root.dataset.homeProgressiveTextBound === 'true') {
          whenTypographyReady(revealAndActivate);
          return;
        }

        const prepare = (element, delay, options = {}) => {
          const {
            flag = 'homeProgressiveText',
            className = 'op-credit--home-name-line',
            animationName = 'home-op-name-enter'
          } = options;

          if (!(element instanceof HTMLElement) || element.dataset[flag] === 'true') {
            return;
          }

          element.dataset[flag] = 'true';
          element.dataset.homeProgressiveState = 'waiting';
          element.style.setProperty('--home-op-delay', `${delay}ms`);
          element.classList.add(className);

          element.addEventListener('animationend', (event) => {
            if (event.animationName !== animationName) {
              return;
            }

            element.dataset.homeProgressiveState = 'entered';
          }, { once: true });
        };

        const prepareAll = (selector, baseDelay, step = 32, prepareTarget = prepare) => {
          root.querySelectorAll(selector).forEach((element, index) => {
            prepareTarget(element, baseDelay + index * step);
          });
        };

        root.dataset.homeProgressiveTextBound = 'true';

        const prepareVisual = (element, delay) => {
          prepare(element, delay, {
            flag: 'homeProgressiveVisual',
            className: 'op-credit--home-visual',
            animationName: 'home-op-visual-enter'
          });
        };

        prepareAll('#profile-top img', 0, 32, prepareVisual);
        prepareAll('#profile-top [data-hscroll-content] > div:first-child', 0);
        prepareAll('#profile-top [data-hscroll-content] li', 32, 32);
        prepareAll('#profile-top [data-hscroll-content] > div:last-child', 128);

        root.querySelectorAll('[data-home-progressive-section]').forEach((section, sectionIndex) => {
          const baseDelay = 160 + sectionIndex * 48;
          const progressiveSelectors = [
            ':scope > article.heti > h5',
            ':scope > article.heti > p',
            ':scope > article.heti > article > p',
            ':scope img',
            ':scope .affiliation-logo',
            '.news-entry > p',
            '.research_h1 > p',
            '.affiliation-meta',
            '.affiliation-role-primary',
            '.affiliation-role-details > summary',
            '.affiliation-role-secondary',
            '.related-link-title',
            '.related-link-actions',
            '.related-link-actions--text'
          ].join(',');

          section.querySelectorAll(progressiveSelectors).forEach((element, index) => {
            const delay = baseDelay + index * 32;

            if (element.matches('img, .affiliation-logo')) {
              prepareVisual(element, delay);
              return;
            }

            prepare(element, delay);
          });
        });

        whenTypographyReady(revealAndActivate);
      };

      const bindHorizontalIndicator = () => {
        const panel = document.querySelector('[data-hscroll-panel]');
        const content = document.querySelector('[data-hscroll-content]');
        const indicator = document.querySelector('[data-hscroll-indicator]');
        if (
          !(panel instanceof HTMLElement) ||
          !(content instanceof HTMLElement) ||
          !(indicator instanceof HTMLElement) ||
          panel.dataset.hscrollBound === 'true'
        ) {
          return;
        }

        let hasScrolledRight = false;

        const updateIndicator = () => {
          if (panel.scrollLeft > 0) {
            hasScrolledRight = true;
          }

          const hasOverflow = content.scrollWidth > panel.clientWidth + 1;
          const shouldShow = hasOverflow && !hasScrolledRight;
          indicator.style.opacity = shouldShow ? '1' : '0';
          indicator.style.visibility = shouldShow ? 'visible' : 'hidden';
        };

        panel.dataset.hscrollBound = 'true';
        updateIndicator();
        requestAnimationFrame(updateIndicator);
        setTimeout(updateIndicator, 120);
        panel.addEventListener('scroll', updateIndicator, { passive: true });
        window.addEventListener('resize', updateIndicator, { passive: true });
        window.addEventListener('load', updateIndicator, { passive: true });

        if ('ResizeObserver' in window) {
          const observer = new ResizeObserver(updateIndicator);
          observer.observe(panel);
        }
      };

      bindProgressiveText();

      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', bindHorizontalIndicator, { once: true });
      } else {
        bindHorizontalIndicator();
      }

      document.addEventListener('astro:page-load', bindProgressiveText);
      document.addEventListener('astro:page-load', bindHorizontalIndicator);
    })();
  </script>
  </br> 
  <hr style="height:1.5px;border-width:0;color:rgba(188, 188, 188, 0.589);background-color:rgba(129, 129, 129, 0.306)"> 

  </LayoutDefault>
